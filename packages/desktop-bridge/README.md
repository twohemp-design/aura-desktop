# @aura/desktop-bridge

Typed contract for the native desktop bridge exposed as `window.auraDesktop`.

This package is intentionally type-only for now. Runtime bridge implementation lives in `apps/desktop/src/preload`.
